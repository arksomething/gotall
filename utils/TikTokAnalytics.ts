// TikTokAnalytics.ts – SDK temporarily disabled after crashes
// Everything here is replaced with inert stubs that do nothing but keep call-sites compiling.

export function initTikTok(): void {
  // SDK disabled – no initialization performed
}

export function trackTikTok(_eventName: string, _params?: Record<string, any>): void {
  // SDK disabled – event ignored
} 